This directory contains a module for cleaning text. Sample inputs and sample outputs can be viewed in this directory.

### Usage:

```
python3 data_processing.py --infile file_name --outfile csv_file_name
```